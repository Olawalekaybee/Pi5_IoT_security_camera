# Pi-Detect — Raspberry Pi dependencies
# pip install -r requirements-pi.txt --break-system-packages
# NOTE: picamera2 installed via apt (see scripts/install_pi.sh)

flask>=3.0,<4.0
opencv-python-headless>=4.9
python-dotenv>=1.0
numpy>=1.26
matplotlib>=3.3
ultralytics>=8.2
psutil>=5.8.0
ultralytics-thop>=2.0.18
